RSpec.describe Array do
  it "array's length is 0" do
    expect(subject.length).to eq(0)
  end

  it "array's length is 1 after push a element" do
    x = 1
    subject.push(x)
    puts subject
    expect(subject.length).to eq(1)
  end
  
end